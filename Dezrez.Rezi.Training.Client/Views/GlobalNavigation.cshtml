@{
    Layout = null;
}

<section class="global-nav-container depth30" id="global-nav-container" data-bind="visible: Visible">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Rezi Auth</a>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav" data-bind="foreach: NavBarRouteItemsBottom">
                    <li data-bind="css: {selected : isActive()}">
                        <!--ko if : nav -->
                        <a data-bind="attr: {href: hash}">
                            <span class="visible-lg visible-md" data-bind="text: title"></span>
                        </a>
                        <!--/ko-->
                    </li>
                </ul>
            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>
</section>